<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
    crossorigin="anonymous"
  />
    <title>Confirm Order</title>
    <style>
      body {
        background-color: #2c3539;
      }
      * {
        font-family: "Itim", cursive;
      }
    
        .form-group {
            background-color: #fff; /* พื้นหลังสีขาว */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* เพิ่มเงา */
            margin: 20px auto; /* จัดกลางในหน้าเว็บ */
            width: 80%; /* กำหนดความกว้าง */
            color: #030303;
        }
        .download-button {
            background-color: #007bff;
            color: #030303;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            margin-top: 10px;
            display: inline-block;
        }
    </style>
</head>
<body>
    <%- include("./../nav.ejs") %>
    <form style="max-width: 720px; margin: 30px auto;"  action="/recept" method="get">
    <h1 class="text-white text-center">รายละเอียดการสั่งซื้อ</h1>
    <div class="form-group ">
        <% payment.forEach(function(paymentItem) { %>
            <p class="card-text">จัดส่งคุณ : <%= paymentItem.PayName %> </p>
            <p class="card-text">ที่อยู่ในการจัดส่ง : <%= paymentItem.PayAddress %> </p>
            <p class="card-text">เบอร์ติดต่อ: <%= paymentItem.PayTel %></p>
            <p class="card-text">จำนวนยอดที่ชำระ : <%= paymentItem.TotalAll %> บาท</p>

            <input type="hidden" name="PayName" value="<%= paymentItem.PayName %>">
            <input type="hidden" name="PayAddress" value="<%= paymentItem.PayAddress %>">
            <input type="hidden" name="PaymentID" value="<%= paymentItem.paymentID %>">
            <input type="hidden" name="TotalAll" value="<%= paymentItem.TotalAll %>">
            <input type="hidden" name="OrderID" value="<%= OrderID %>">
            <input type="hidden" name="PayTel" value="<%= PayTel %>">
            <button type="submit" class="btn btn-primary">ใบเสร็จการชำระเงิน</button>
        </form>
        <% }); %>    
    </div>
    </form>
    <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
    crossorigin="anonymous"
  ></script>
</body>
</html>
